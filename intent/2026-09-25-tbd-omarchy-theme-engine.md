---
status: draft
issue: tbd
author: olafkfreund
---

# Intent: Omarchy runtime theme engine for NixOS

## Problem

The current NixOS configuration uses Omarchy as the runtime theme source and
Stylix as the rebuild-time renderer. Changing an Omarchy theme updates
Omarchy-owned files immediately, but GTK, Qt, terminals, browsers, Flatpak
applications, and other user applications do not all change together without
manual reloads or a NixOS rebuild. Stylix-generated files are immutable store
outputs, while Omarchy's theme engine expects mutable user-session files.

## Proposed outcome

One Omarchy theme change updates the complete running desktop palette through a
single NixOS-packaged runtime engine and menu plugin, without requiring
`nixos-rebuild` for user-session applications. The engine reloads or restarts
applications where live theme reload is not supported and reports remaining
stale applications. Stylix remains the declarative renderer for rebuild-time
and boot-time surfaces such as GRUB, Plymouth, fonts, icons, cursor, and other
static targets.

## Affected users and systems

- The `olafkfreund` desktop user.
- Nixarchy/Omarchy sessions on p620, razer, and p510.
- Omarchy theme hooks and Quickshell menu plugins.
- GTK, Qt/KDE, terminals, browsers, Electron applications, and Flatpak apps.
- Existing Stylix integration in the NixOS configuration.

## Constraints

- Omarchy `colors.toml` remains the runtime palette source of truth.
- No runtime writes to `/nix/store` or Nix-generated immutable files.
- NixOS and Home Manager integration remains declarative.
- The engine runs as the desktop user with no unnecessary privilege.
- Runtime-owned files are not also generated by Stylix.
- Flatpak access is explicit and opt-in because it changes sandbox grants.
- Bootloader, initrd, Plymouth, and console artifacts may still require a
  rebuild or reboot.
- The menu plugin delegates theme application to the engine.
- Existing NixOS configuration changes remain untouched.

## Open questions

- Which applications should be restarted automatically versus reported stale?
- Should the first implementation package Omarchroma directly or implement a
  smaller Nix-native runtime engine using its palette and renderer model?
- Should the plugin remain in this repository or become a separate repository?
- What issue number should replace `tbd` once tracking is established?
